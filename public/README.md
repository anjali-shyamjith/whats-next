# Frontend Files

This directory (`public/`) is reserved for the frontend files.

Place your HTML, CSS, and JS files here. For example:
- `index.html`
- `style.css`
- `script.js`

When the backend is running, these files will be served statically at the root URL (e.g., `http://localhost:3000/`).
